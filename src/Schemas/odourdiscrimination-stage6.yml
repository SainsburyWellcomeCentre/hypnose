%YAML 1.1
---
# yaml-language-server: $schema=sequence-schema.json

nextSequence: Schemas/stage6-test.yml
performaceCriterion: 1.1
sequences: [
    [{name: OdourDiscriminationA_Stage6,
    defaultCommand: Default,
    interCommand: Purge,
    interCommandTime: 0.2,
    maximumTime: 10,
    responseTime: 99999,
    interTrialInterval: 0,
    completionRequiresEngagement: True,
    rewardConditions: [
      {position: 0, definition: [
        [{command: OdorA, rewarded: True, minimumSamplingTime: 0.35, maximumSamplingTime: 1}]
      ]}
    ],
    rewardAttempts: 0,
    enableTrialIndicator: True,
    enableRewardLocationIndicator: False,
    resetOnReward: True,
    skipSampling: False
    }],
    [{name: OdourDiscriminationB_Stage6,
    defaultCommand: Default,
    interCommand: Purge,
    interCommandTime: 0.2,
    maximumTime: 10,
    responseTime: 99999,
    interTrialInterval: 0,
    completionRequiresEngagement: True,
    rewardConditions: [
      {position: 1, definition: [
        [{command: OdorB, rewarded: True, minimumSamplingTime: 0.35, maximumSamplingTime: 1}]
      ]}
    ],
    rewardAttempts: 0,
    enableTrialIndicator: True,
    enableRewardLocationIndicator: False,
    resetOnReward: True,
    skipSampling: False
    }]
]
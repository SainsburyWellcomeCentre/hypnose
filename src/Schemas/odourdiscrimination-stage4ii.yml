%YAML 1.1
---
# yaml-language-server: $schema=sequence-schema.json

nextSequence: Schemas/rig1-odourdiscrimination-stage5-test.yml
performaceCriterion: 1.1
sequences: [
    [{name: OdourDiscriminationA_Stage4ii,
    defaultCommand: Default,
    interCommand: Purge,
    interCommandTime: 0.2,
    maximumTime: 10,
    responseTime: 2.2,
    interTrialIntervalSuccessfulTrial: 0,
    interTrialIntervalUnsuccessfulTrial: 0,
    completionRequiresEngagement: True,
    rewardConditions: [
      {position: 0, definition: [
        [{command: OdorA, rewarded: True, index: 0, minimumSamplingTime: 0.3, maximumSamplingTime: 1}]
      ]}
    ],
    rewardAttempts: 99999,
    enableTrialEndIndicator: False,
    enableRewardLocationIndicator: True,
    resetOnReward: True,
    skipSampling: False,
    enableTrialStartIndicator: True
    }],
    [{name: OdourDiscriminationB_Stage4ii,
    defaultCommand: Default,
    interCommand: Purge,
    interCommandTime: 0.2,
    maximumTime: 10,
    responseTime: 2.2,
    interTrialIntervalSuccessfulTrial: 0,
    interTrialIntervalUnsuccessfulTrial: 0,
    completionRequiresEngagement: True,
    rewardConditions: [
      {position: 1, definition: [
        [{command: OdorB, rewarded: True, index: 0, minimumSamplingTime: 0.3, maximumSamplingTime: 1}]
      ]}
    ],
    rewardAttempts: 99999,
    enableTrialEndIndicator: False,
    enableRewardLocationIndicator: True,
    resetOnReward: True,
    skipSampling: False,
    enableTrialStartIndicator: True
    }]
]
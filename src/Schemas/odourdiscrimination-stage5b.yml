%YAML 1.1
---
# yaml-language-server: $schema=sequence-schema.json

nextSequence: Schemas/rig1-odourdiscrimination-stage5-test.yml
performaceCriterion: 1.1
sequences: [
    [{name: OdourDiscriminationA_Stage5b,
    defaultCommand: Default,
    interCommand: Purge,
    interCommandTime: 0.2,
    maximumTime: 10,
    responseTime: 2.25,
    completionRequiresEngagement: True,
    rewardConditions: [
      {position: 0, definition: [
        [{command: OdorA, rewarded: True, index: 0, minimumSamplingTime: 0.25, maximumSamplingTime: 1}]
      ]}
    ],
    rewardAttempts: 0,
    enableTrialStartIndicator: True,
    enableTrialEndIndicator: False,
    enableRewardLocationIndicator: False,
    resetOnReward: True,
    interTrialIntervalSuccessfulTrial: 0,
    interTrialIntervalUnsuccessfulTrial: 0,
    skipSampling: False
    },
    {name: OdourDiscriminationB_Stage5b,
    defaultCommand: Default,
    interCommand: Purge,
    interCommandTime: 0.2,
    maximumTime: 10,
    responseTime: 2.25,
    completionRequiresEngagement: True,
    rewardConditions: [
      {position: 1, definition: [
        [{command: OdorB, rewarded: True, index: 0, minimumSamplingTime: 0.25, maximumSamplingTime: 1}]
      ]}
    ],
    rewardAttempts: 0,
    enableTrialStartIndicator: True,
    enableTrialEndIndicator: False,
    enableRewardLocationIndicator: False,
    resetOnReward: True,
    interTrialIntervalSuccessfulTrial: 0,
    interTrialIntervalUnsuccessfulTrial: 0,
    skipSampling: False
    }]
]
